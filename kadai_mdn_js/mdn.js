var date = new Date();         
var yyyy = date.getFullYear(); 
var mm = date.getMonth() + 1;  
var dd = date.getDate(); 
var w = date.getDay();   
 
  
var result = yyyy + "年" + mm + "月" + dd + "日" ; 
 
console.log(result);